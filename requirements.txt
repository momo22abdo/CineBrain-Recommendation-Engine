# ── Backend (FastAPI) ──────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0          # use faiss-gpu if you have CUDA
numpy>=1.26.0
pandas>=2.2.0
httpx>=0.27.0             # async Gemini calls
scikit-learn>=1.4.0       # still used for CountVectorizer in preprocessing

# ── Frontend (Streamlit) ───────────────────
streamlit>=1.35.0
requests>=2.31.0
